name: Deploy Forumline Central Services

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'central-services/**'

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      project-id: prj_bIR1T3sDDe3bhKQjLHH7WGlXuKCu
      working-directory: central-services
    secrets: inherit
