{
  "name": "forumline",
  "private": true,
  "workspaces": ["demo", "hub", "packages/*"],
  "scripts": {
    "tauri:dev": "TAURI_DIR=desktop tauri dev",
    "tauri:build": "TAURI_DIR=desktop tauri build"
  }
}
