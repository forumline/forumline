name: Deploy Forumline Demo

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'forum-demo/**'

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      project-id: prj_Q6NCsZIw3eRbYLQFMTkLon3wZ3Nk
      working-directory: forum-demo
    secrets: inherit
