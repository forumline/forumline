Multi-tenant hosted forum platform (*.forumline.net).

- `hosted/` — Main server entrypoint, tenant routing, SPA serving
- `forum/` — Forum engine (handlers, services, stores, models)
- `platform/` — Platform API (provisioning, export/import, custom sites)
- `src/` — Default forum frontend (vanilla JS, Vite, TailwindCSS)
