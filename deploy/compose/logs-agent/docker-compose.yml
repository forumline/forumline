services:
  vector:
    image: timberio/vector:0.45.0-alpine
    restart: unless-stopped
    volumes:
      - /opt/logs-agent/vector.toml:/etc/vector/vector.toml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: ["--config", "/etc/vector/vector.toml"]
    network_mode: host
