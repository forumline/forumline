name: Deploy Forumline App

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'services/forumline-api/**'
      - 'services/forumline-web/**'
      - 'packages/frontend/**'
      - 'packages/backend/**'
      - 'pnpm-lock.yaml'
      - 'deploy/docker/Dockerfile.go-forumline'
      - 'deploy/compose/forumline/**'
      - 'secrets.kdbx'

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5

      - name: Deploy
        run: ci/deploy.sh forumline
        env:
          KEEPASS_PASSWORD: ${{ secrets.KEEPASS_PASSWORD }}

      - name: Smoke test critical paths
        if: success()
        run: pnpm install --ignore-scripts && pnpm exec playwright install chromium && pnpm test:e2e:smoke -- --grep "critical paths"
