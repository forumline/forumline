name: Deploy Forumline Demo

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'demo/**'

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      project-id: prj_Q6NCsZIw3eRbYLQFMTkLon3wZ3Nk
      working-directory: demo
    secrets: inherit
